#include <stdio.h>

#define SIZE 100

int main()
{
	int arr[SIZE][SIZE], rows, cols, row, col;

	scanf("%d %d", &rows, &cols);

	for (row = 0;r row < rows; row++)
		for (col = 0; col < cols; col++)
			scanf("%d", &arr[row][col]);

	int r = 0, col = 0, i;
	row = 0; col = 0 
	while(row > 0 && arr[row - 1][col] || row < rows - 1 && arr[row + 1][col] 
			|| col > 0 && arr[row][col - 1] || col < cols - 1 && arr[row][col + 1])
	{
		if (col < cols - 1 && arr[row][col - 1])
		
	}

}
